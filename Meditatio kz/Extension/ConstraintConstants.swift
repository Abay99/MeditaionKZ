//
//  ConstraintConstants.swift
//  Meditatio kz
//
//  Created by Nazhmeddin Babakhanov on 10/10/2019.
//  Copyright © 2019 Nazhmeddin Babakhanov. All rights reserved.
//

import Foundation


import UIKit

struct ConstraintConstants {
    
    static var tableViewFromToCellTextFieldMultiplier = 0.245
    
    static var tableViewCellOffset = ConstraintConstants.s24
    
    static let width = UIScreen.main.bounds.width
    static let height = UIScreen.main.bounds.height
    
    static let h196 = (196 / 375) * ConstraintConstants.width
    static let s14 = (14 / 375) * ConstraintConstants.width
    static let s16 = (16 / 375) * ConstraintConstants.width
    static let s15 = (15 / 667) * ConstraintConstants.height
    static let s20 = (20 / 375) * ConstraintConstants.width
    static let s30 = (30 / 375) * ConstraintConstants.width
    static let s35 = (35 / 375) * ConstraintConstants.width
    static let s40 = (40 / 375) * ConstraintConstants.width
    static let s50 = (50 / 375) * ConstraintConstants.width
    static let s160 = (160 / 375) * ConstraintConstants.width
    static let s64  = (64 / 375) * ConstraintConstants.width
    static let s96  = (96 / 375) * ConstraintConstants.width
    static let s210 = (210 / 375) * ConstraintConstants.width
    
    static let h2 = (2 / 667) * ConstraintConstants.height
    static let h5 = (5 / 667) * ConstraintConstants.height
    static let w8 = (8 / 375) * ConstraintConstants.width
    static let w6 = (6 / 375) * ConstraintConstants.width
    static let w25 = (25 / 375) * ConstraintConstants.width
    static let h35 = (35 / 667) * ConstraintConstants.height
    static let h10 = (10 / 667) * ConstraintConstants.height
    static let h8 = (8 / 667) * ConstraintConstants.height
    static let h20 = (20 / 667) * ConstraintConstants.height
    static let h100 = (100 / 667) * ConstraintConstants.height
    static let h110 = (110 / 667) * ConstraintConstants.height
    static let h230 = (230 / 667) * ConstraintConstants.height
    static let h95 = (95 / 667) * ConstraintConstants.height
    static let h90 = (90 / 667) * ConstraintConstants.height
    static let h80 = (80 / 667) * ConstraintConstants.height
    static let h70 = (70 / 667) * ConstraintConstants.height
    static let h75 = (75 / 667) * ConstraintConstants.height
    static let h105 = (105 / 667) * ConstraintConstants.height
    static let s10 = (10 / 375) * ConstraintConstants.width
    static let w15 = (15 / 375) * ConstraintConstants.width
    static let w13 = (13 / 375) * ConstraintConstants.width
    static let w16 = (16 / 375) * ConstraintConstants.width
    static let w18 = (18 / 375) * ConstraintConstants.width
    static let s12 = (12 / 375) * ConstraintConstants.width
    static let s8  = (8 / 375)  * ConstraintConstants.width
    static let s6  = (6 / 375)  * ConstraintConstants.width
    static let h15 = (15 / 667) * ConstraintConstants.height
    static let h17 = (17 / 667) * ConstraintConstants.height
    static let h25 = (25 / 667) * ConstraintConstants.height
    static let h40 = (40 / 667) * ConstraintConstants.height
    static let h45 = (45 / 667) * ConstraintConstants.height
    static let w20 = (20 / 375) * ConstraintConstants.width
    static let w60 = (60 / 375) * ConstraintConstants.width
    static let w70 = (70 / 375) * ConstraintConstants.width
    static let w80 = (80 / 375) * ConstraintConstants.width
    static let w110 = (110 / 375) * ConstraintConstants.width
    static let w120 = (120 / 375) * ConstraintConstants.width
    static let h60 = (60 / 667) * ConstraintConstants.height
    static let h55 = (55 / 667) * ConstraintConstants.height
    static let s24 = (24 / 375) * ConstraintConstants.width
    static let w30 = (30 / 375) * ConstraintConstants.width
    static let w35 = (35 / 375) * ConstraintConstants.width
    static let h30 = (30 / 667) * ConstraintConstants.height
    static let w65 = (60 / 375) * ConstraintConstants.width
    static let s44 = (44 / 375) * ConstraintConstants.width
    static let w40 = (40 / 375) * ConstraintConstants.width
    static let s48 = (48 / 375) * ConstraintConstants.width
    static let w50 = (50 / 375) * ConstraintConstants.width
    static let h50 = (50 / 667) * ConstraintConstants.height
    static let s5  = (5 / 375) * ConstraintConstants.width
    static let s52 = (52 / 375) * ConstraintConstants.width
    
    static let defaultCellHeight = ConstraintConstants.width * 0.176
    static let underlinedCellHeight = ConstraintConstants.width * 0.1
    
    static let seeMoreCellHeight = ConstraintConstants.height * 0.45
    static let seeMoreCellWidth = ConstraintConstants.width * 0.45
    
    static let w90 = (90 / 375) * ConstraintConstants.width
    static let w125 = (125 / 375) * ConstraintConstants.width
    static let w130 = (130 / 375) * ConstraintConstants.width
    static let w150 = (150 / 375) * ConstraintConstants.width
    static let w190 = (190 / 375) * ConstraintConstants.width
    static let w220 = (220 / 375) * ConstraintConstants.width
    static let w300 = (300 / 375) * ConstraintConstants.width
    static let h200 = (200 / 667) * ConstraintConstants.height
    static let h150 = (150 / 667) * ConstraintConstants.height
    static let h130 = (130 / 667) * ConstraintConstants.height
    static let h120 = (120 / 667) * ConstraintConstants.height
    static let s130 = (130 / 375) * ConstraintConstants.width
    static let h160 = (160 / 667) * ConstraintConstants.height
    static let h220 = (220 / 667) * ConstraintConstants.height
    static let h260 = (260 / 667) * ConstraintConstants.height
    static let h300 = (300 / 667) * ConstraintConstants.height
    static let h400 = (400 / 667) * ConstraintConstants.height
    static let h450 = (450 / 667) * ConstraintConstants.height
    static let h540 = (540 / 667) * ConstraintConstants.height
    static let s98  = (98 / 375) * ConstraintConstants.width
    static let s343 = (343 / 375) * ConstraintConstants.width
    static let s80  = (80 / 375) * ConstraintConstants.width
    static let s60  = (60 / 375) * ConstraintConstants.width
    static let s92  = (92 / 375) * ConstraintConstants.width
    static let s112 = (112 / 375) * ConstraintConstants.width
    static let s128 = (128 / 375) * ConstraintConstants.width
    static let s105 = (105 / 375) * ConstraintConstants.width
    
    static let alpha: CGFloat = 0.3
}
